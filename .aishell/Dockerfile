# Project-specific extension
# Must start FROM aishell:foundation
FROM aishell:base

# Example: install project-specific tools
# RUN apt-get update && apt-get install -y some-tool

# Example: add project-specific scripts
# COPY scripts/ /usr/local/bin/
